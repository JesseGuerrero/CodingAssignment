import React from 'react';

const Navigation = () => {
    return (
        <div style={{width: "100%"}}>
            <div style={{margin: "0 auto"}} border="1">
                {'<<'}prevous - next{'>>'}
            </div>
        </div>
    );
};

export default Navigation;
